4.7

使用sys.insert可以解决import的一些问题
另外发现了一个中文caption的数据集,在那个ruotianluo的self critic 的issue下,有个人fork了他的仓库,点开那个人就发现了.

4.8
如果在服务器上使用matplotlib出错,那么就 import matplotlib as mpl   mpl.use('Agg')

4.15
torch使用其他gpu:CUDA_VISIBLE_DEVICES=2

5.4
完成了毕设基本的代码,接下来开始实验,这是个值得纪念的日子

torch 的unsequence的作用相当于增加一个维度,比如unsequence(1)
torch.bmm(batch1,batch2)作用就是把两个三维的tensor矩阵相乘,比如BxNxM,BxMxP,得到BxNxP
在使用dict的时候,能不使用dict['key']就不使用,尽量用get方法,因为当其不存在时,还可以设置缺省值,从而避免低级错误

测试


